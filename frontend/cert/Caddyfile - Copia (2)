https://localhost {
    tls cert.pem key.pem

    # Backend HTTPS (Spring Boot)
    reverse_proxy /api/* https://localhost:8443 {
        transport http {
            tls_insecure_skip_verify
        }
    }

    # Frontend HTTP (http-server)
    reverse_proxy /* http://localhost:8080
}